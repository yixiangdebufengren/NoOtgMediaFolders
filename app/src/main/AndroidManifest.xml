<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- LSPosed 模块标识 -->
    <application
        android:allowBackup="false"
        android:label="No OTG Media Folders"
        android:supportsRtl="true">

        <!-- LSPosed 模块元数据：xposedmodule=true, xposedminversion, xposedscope -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="Prevent MediaProvider from auto-creating default dirs (DCIM/Download/...) on USB OTG and SD card storage. Android 16 only." />
        <meta-data
            android:name="xposedminversion"
            android:value="82" />
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope" />

        <!-- 空壳 Launcher Activity，用于让 LSPosed 正确识别该模块 -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>